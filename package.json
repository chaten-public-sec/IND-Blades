{
  "name": "ind-blades-dashboard",
  "version": "1.0.0",
  "description": "Full-stack dashboard for IND Blades",
  "scripts": {
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client",
    "bot": "python -u bot.py",
    "dev": "concurrently -k -n API,WEB,BOT -c blue,green,magenta \"npm run server\" \"npm run client\" \"npm run bot\"",
    "dashboard": "concurrently -k -n API,WEB -c blue,green \"npm run server\" \"npm run client\"",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
